services:
  - type: web
    name: emr-simulator
    env: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: SESSION_SECRET
        generateValue: true
      - key: PORT
        value: 10000
